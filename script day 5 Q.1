For the given JSONiterate over all for loops(for,for in,of,of for each)?

     var numbers =[10,20,30,40,50]
     for (var i=0;i<numbers .leanth;i++) {
        console.log(numbers[i])
     }

     For in loop:
       var person ={
        fname:"Nick",
        Iname:"jonas",
        age :25
       };
       for (let x in person){
        console.log(x +"; "+person[x])
       }